This directory contains content and files pertaining to the BWA to samtools to pilon pipeline.

The input files that are actually relevant are the 2 raw assembly fasta files and the 4 trimmomatic fastq.gz files.

Main output produces a polished fasta file for each mutant

Scripts contain a separate pipeline script for each mutant, so both can be run simultaneously.